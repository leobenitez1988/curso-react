import React from 'react'
import ReactDOM from 'react-dom'
import Card from './components/Card'

const element = <Card></Card>

const container = document.getElementById('root')
ReactDOM.render(element, container)